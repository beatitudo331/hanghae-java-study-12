//2-4.

정답 :

byte b = 256;
-> byte는 최대 127까지 가능하다.
char c = '';
''은 문자가 아니기 때문에 char로 지정할 수 없다. ' ' 로 띄어쓰기해야한다.
char answer = 'no';
char 형식은 글자 하나만 받는다. 둘 이상을 받으려면 String 타입을 사용한다.
float f = 3.14
코드 마지막에 ; 추가해야한다.