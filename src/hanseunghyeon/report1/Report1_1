//2-4. 다음 중 변수를 잘못 초기화 한 것은?

byte b = 256;           // 오답 : 바이트의 범위를 벗어났다. (-128 ~ 127)
char c = '';            // 오답 : 문자는 ''안에 반드시 문자가 하나 이상 들어가야 한다.
char answer = 'no';     // 오답 : char는 문자열 하나만 입력 가능
float f = 3.14          // 오답 : 실수형의 기본은 double이므로 float는 리터럴에 f를 붙여야함.
double d = 1.4e3f;

