2-4번 문제

정답 : 1, 2, 3, 4
 1) byte의 범위는 -128 ~ 127 이다.
 2) char은 비어있으면 안 되며, 한 문자로 초기화 해야 한다.
 3) char은 두 문자로 초기화 하면 안 된다.
 4) float로 초기화할 때, 접미사 f를 붙이거나 형 변환 해야한다.
 5) double(8byte)에 float(4byte)를 넣는 것이므로 괜찮다.

