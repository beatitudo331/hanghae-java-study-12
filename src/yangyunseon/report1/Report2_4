2-4번 문제 다음 중 변수를 잘못 초기화 한 것은?
byte b = 256;
char c = '';
char answer = 'no';
float f = 3.14
double d = 1.4e3f;

정답 :
byte b = 256; // byte는 1바이트로 표현된 값이기 때문에 -128~127까지만 표현 가능
char c =''; // char는 아무것도 없는 걸로 초기화 하는 건 불가능 하고, ' '이런식으로 공백을 넣어줘야한다.
char answer = 'no'; // char는 2바이트라 문자 하나만 가능하여 두 글자는 불가능
float f = 3.14 //세미콜론도 안 쓰고 F도 안 붙여서 탈락!
