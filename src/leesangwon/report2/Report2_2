3-2번 문제


정답 :
true    // '||'는 or에 해당하며, 두 피연산자 중 어느 한 쪽만 true이어도 true를 결과로 얻는다.
13      // y += 10 -> y=y+10, x++ -> x 후위형으로 값이 참조된 후 증가 하여 변화가 없다
5       // x += 2 -> x=x+2  앞연산자에서 x++증가 되어 3이되어 합이 5가 된다.
false   // c가 소문자이기때문에
2       // 65 - 63 문자코드
5       // 35 - 30 문자코드
66      // 문자 코드값 65 +1
B       //참조값이 증가 하여 B
B       //선행으로 증가하여 B
C       //후행으로 증가하여 C